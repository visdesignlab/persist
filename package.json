{
  "name": "notebook-vis-integration",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "setup": "npx --yes stmux -w error -e ERROR -m system -M -- [ \"npm run setup:reapply\" : \"npm run setup:ext\" ]",
    "setup:reapply": "cd reapply && poetry install && cd ..",
    "setup:ext": "cd interactivede && jupyter lab build --minimize=False && pip install -e . && jupyter labextension develop --overwrite . &&  cd ..",
    "dev": "npx --yes stmux -w always -e ERROR -m system -M -t \"Dev\" -- [ \"npm run dev:ext\" .. \"npm run dev:lab:auto\" ]",
    "dev:ext": "npm run watch --prefix interactivede",
    "dev:lab": "jupyter lab",
    "dev:lab:auto": "jupyter lab --autoreload --minimize=False",
    "bump:reapply": "./scripts/bump-reapply-version.sh",
    "bump:ext": "./scripts/bump-ext-version.sh",
    "bump": "concurrently -g -n 'BUMP: ' -c 'bgRed.bold,bgCyan.bold' npm:bump:*",
    "publish:reapply": "cd reapply && poetry build && . /home/vscode/pypi/pypirc && poetry publish && cd ..",
    "publish:ext": "cd interactivede && jlpm build && jlpm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kirangadhave/notebook-vis-integration.git"
  },
  "author": "Kiran Gadhave",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kirangadhave/notebook-vis-integration/issues"
  },
  "homepage": "https://github.com/kirangadhave/notebook-vis-integration#readme",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
